# Original Image Rules

- Preserve high-resolution or unused image sources here for future regeneration.
- Mirror the closest runtime category when possible, for example `icons/upgrades/` or `sprites/player/`.
- Do not add this directory to `pubspec.yaml`.
