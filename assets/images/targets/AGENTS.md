# Target Image Rules

- Store only optimized runtime target images.
- Use `target_stage_N.png` for durability/hit-count visual stages.
- Keep large originals and chromakey variants in `source-assets/original-images/targets/`.
