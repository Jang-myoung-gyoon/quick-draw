# Sprite Asset Rules

- Store optimized runtime sprites only.
- Use `player/` for the main character and `enemies/` for enemy sprites.
- Prefer sprite sheets for animations with more than a few frames.
- Keep generated raw frames, failed attempts, chromakey references, and videos in `source-assets/original-images/`.
