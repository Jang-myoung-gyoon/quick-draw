# Player Sprite Sheet Rules

- Store optimized player animation sheets used at runtime.
- Keep frame dimensions and grid layout documented in the consuming component tests.
- Raw frame sequences belong in `source-assets/original-images/`.
