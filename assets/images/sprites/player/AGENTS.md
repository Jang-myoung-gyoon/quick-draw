# Player Sprite Rules

- Put single-frame player runtime sprites in `actions/`.
- Put repeated or long player animations in `sheets/`.
- Use stable names based on gameplay state, not model names or generation attempts.
- Keep source frames and unused motion attempts outside the app bundle.
