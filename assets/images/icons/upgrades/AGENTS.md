# Upgrade Icon Rules

- Store one final transparent PNG per `UpgradeType`.
- File names must match the upgrade concept in snake_case, for example `blade_power.png`.
- Keep chromakey/source variants in `source-assets/original-images/icons/upgrades/`.
