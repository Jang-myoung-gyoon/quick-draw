# Icon Asset Rules

- Runtime icons should live in a purpose-specific subdirectory such as `upgrades/`.
- App icon concepts, favicon source images, and generated size variants belong in `source-assets/original-images/icons/` unless the app references them directly.
- Do not keep chromakey icon variants in runtime assets.
