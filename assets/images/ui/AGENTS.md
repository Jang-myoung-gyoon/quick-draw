# UI Image Rules

- Store only images shown by Flutter UI overlays, loading screens, or menus.
- Use names like `home_background.png`, `home_title_battoujutsu.png`, and `loading_chase.png`.
- Move alternate concepts, chromakey versions, and unused title/background attempts to `source-assets/original-images/concepts/`.
